/**
 * App Configuration
 * 
 * Configures Nuxt UI theme and colors for GabeBet.
 */

export default defineAppConfig({
  ui: {
    colors: {
      primary: 'emerald',
      neutral: 'slate',
    },
  },
})
